def initiate_payment(user_id, amount, phone_number):
    # Fake API stub
    return {
        "status": "success",
        "message": f"Payment of {amount} TZS initiated to {phone_number} for user {user_id}"
    }
